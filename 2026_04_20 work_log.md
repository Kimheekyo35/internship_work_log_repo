# 2026_04_20 work_log

## playwright MCP를 사용한 후, 토큰 문제

playwright mcp를 계속 켜두면 Token이 너무많이 소비되는 문제가 발생했음. 그래서 사용하지 않을 때는 비활성화 시켜야 함.

## 설치과정 정리
`claude mcp add playwright -s user -- npx @executeautomation/playwright-mcp-server`

이후
1. /home/linuxuser/.claude.json 와 같은 경로에 들어가면
```
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest"
      ]
    }
  }
}
```
확인 가능하지만 이 부분을 계속 활성화시키면 Token이 너무 빨리 닳는 문제가 발생할 수 있음. 
평소에는

```
{
  "mcpServers": {
    "_playwright_disabled": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest"
      ]
    }
  }
}
```
로 두면 MCP 클라이언트가 해당 이름을 인식 못 해서 실행되지 않음.

## 약 2시간 정보 소요되던 크롤링 -> 1시간 단축

국가별로 금액과 순위를 갖고 와야 하기 때문에 각 페이지를 열 때마다 국가 선택을 해야했음. 또 Yesstyle의 경우 보안때문에 seleniumBase 아니면 매 브라우저를 껐다 켜는 방식으로 catcha 를 피했어야 함.
수정한 방법은 국가별로 Dialof의 정보 (국가, 고유 번호) 를 쿠키로 저장하여 앞으로 브라우저 열 때마다 그 쿠키를 사용하게끔 수정함.

🥱 이후, 로컬에서 실행해봤지만 속도 부분에서 개선되지 않았음
tage 상품이 yesstyle에 업로드된 상품의 개수가 5개 임을 알아서 tage의 상세 정보를 담는 dict의 길이가 5를 넘어가면 바로 break 하게 함.
전체 페이지를 다 돈 것도 tage 상품을 가져오기 위함이었는데 이미 다 채웠으면 나머지 페이지는 돌 필요가 없어서....

Yesstyle에 상품이 등록될 때마다 수정해야하는 귀찮음이 있어서 추가하고 싶지 않았지만 크롤링 시간이 너무 많이 증가하는 문제 발생 💢
약 10분만에 끝남
