# 2026_02_12 work_log

## 오늘 배운점

### 문제점
1. 특정 국가에 First Mile Shipping Method에 코드를 작성해도 Commit이 안 눌러지는 문제점 발생
2. Streamlit를 깃허브에 올려서 배포하려고 했지만 `쿠키` 업데이트를 못하는 문제점.. 갱신이 안 됨
3. 서버에서 Streamlit 배포했지만 서버의 로컬에 열려있어서 방화벽 문제

### 해결방법
1. Delivery 방식을 Self 로 바꿔야 함 -> playwright로 조작하여 Self로 바꾸게 변경
2. 서버에서 Streamlit으로 배포하여 쿠키 문제 해결
3. 방화벽 문제 -> `sudo ufw status` 로 모든 포트 방화벽 권한 체크
  * `ss -lntp | grep 8501` : 로 켜져있는지 확인
  * `sudo ufw reload` : 다시 streamlit 켬
