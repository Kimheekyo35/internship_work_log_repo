# 2026_02_09 work_log

## 오늘 배운 점

### airflow에서 permission denied가 떴을 때
  - `ls -ld /home/linuxuser/wemarketing/메타광고현황수집/logs/password_manager.log` 를 입력하여 권한을 알아냄
  - 나의 경우, `airflow` -> 컨테이너 실행 유저
  - `linuxuser` -> 서버 폴더 소유자
  - 컨테이너 실행유저가 권한이 없어서 deny가 발생한 것
```
sudo chown -R 50000:0 /home/linuxuser/..../..
```
처럼 작성하여 권한 부여하기

➕ `root` 는 리눅스에서 **최고 관리자** 임.

### git stash (임시저장) 하는 방법
`git stash push -m "처음"` 로 git pull 하기 전에 stash하여 저장
`git stash list` 하여 이름과 index 번호 찾기
`git stash pop stash@{0}` 하면 불러오기 + 삭제까지 진행 가능함

### python 에서 pdf text 추출 방법
- PyMuPDF 라이브러리 사용하여 pdf 기반 text 추출 진행

```
def pdf_to_text(file_path):
    files = Path(file_path).glob("*.pdf")
    for file in files:
        doc = fitz.open(file)
        for page_num in range(len(doc)):
            page = doc.load_page(page_num)
            text = page.get_text()
            print(f"--- Page {page_num + 1} ---")
            for line in text.splitlines():
                line = line.replace("Order ID: ", "")
                line = line.replace("Tracking NO.: ", "")
                list_text.append(line)
            # 리스트에서 Items: 제거
            list_text.remove("Items:")
            break  # 첫 페이지만 출력
        break  # 첫 파일만 출력
```
으로 코드 작성하여 지정한 파일내에 있는 pdf 파일들을 읽고 송장번호, 주문번호만 남기기 위해 Order ID, Tracking No를 제거함
## 💢 고민점

하지만 pdf 자체를 excel화 해야 했음 (텍스트 추출 ❌)
adobe pdf API 가 존재하지만, 월 500건 api request 한도가 있고.. 그 이후에는 건별 돈이 나가서 어떻게 엑셀로 자동화하여 전달해야하는 지 더 고민해봐야겠음

➕ 익일 업무 예정
---
1. 공유 파일 권한 받아서 저장 경로 수정
2. 기존 코드 서버로 이전
3. excel로 바꿀 수 있는 라이브러리 더 찾아보기
