# 2026_01_23 work_log

## 문제점
최상위 폴더의 이름을 고쳤더니 경로 충돌 문제 발생
-> airflow, db를 제대로 찾지 못함

1. airflow
```
deactivate 2>/dev/null || true
cd /home/linuxuser/wemarketing/Airflow_project
source airflow_env/bin/activate
which python
python -m airflow version
```

-> 다음과 같이 설정하여 airflow의 경로를 바꿀 수 있음

2. DB 경로 찾기
export AIRFLOW__CORE__DAGS_FOLDER=/home/linuxuser/wemarketing/Airflow_project/airflow/dags

로 수정하면 AIRFLOW의 DB가 제대로 찾을 수 있음.

3. apache-airflow 설치 코드 (공식 문서)
```
pip install "apache-airflow[celery]==3.1.6" --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-3.1.6/constraints-3.10.txt"
```

4. PATH를 꼼꼼하게 읽어야될 듯
PATH를 보면 내가 어떤 가상환경에 접속되어있고, 어떤 라이브러리를 어느 가상환경에 설치했는지 알 수 있는데 제대로 안 봄

## AI Prompt를 어떻게 짜야 효율적으로 짤 수 있는지 고민하기 + AI를 제대로 활용하는 방법 생각하기
